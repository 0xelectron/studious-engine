#include<cs50.h>
#include<stdio.h>
#include<stdlib.h>

int* get_age(void);

int main(int argc,char* argv)
{
    if (arc != 2)
    {
        printf("Usage: ./trainer dolphins\n");
        return(1);
    }
int n = atoi(argv[1]);

if(n<1)
{
    printf("please provide +ve integer!");
    return(1);
}
int ages[n];
for(int i=0;i<n;i++);
{
    ages[i] = get_age();
    
}

for (int i=0; i<n;i++);
{
    if (ages[i] == 
}


}
